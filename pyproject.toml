[project]
name = "c-elegans"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "datasets>=4.8.4",
    "einops>=0.8.2",
    "numpy>=2.4.4",
    "tokenizers>=0.22.2",
    "torch>=2.11.0",
]
